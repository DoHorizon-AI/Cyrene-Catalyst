[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "cyrene-catalyst"
version = "0.1.0"
description = "Cyrene Product-owned dataset and lineage contract with a replaceable data engine"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
  "httpx==0.28.1",
  "fastapi==0.141.1",
  "pydantic==2.13.5",
  "uvicorn==0.37.0",
  "cyrene-artifacts @ git+https://github.com/DoHorizon-AI/Cyrene-Platform.git@c59be6f2bd82489fbe933dadff84fc589e00afd9#subdirectory=sdk/python/cyrene_artifacts",
  "cyrene-plugin-runtime @ git+https://github.com/DoHorizon-AI/Cyrene-Plugins-Official.git@3afbac4d386eb7a27f6778149187884820c0b7f6#subdirectory=sdk/python/cyrene_plugin_runtime",
]

[project.scripts]
cyrene-catalyst = "cyrene_catalyst.cli:main"

[dependency-groups]
dev = [
  "cyrene-dataset-preparation @ git+https://github.com/DoHorizon-AI/Cyrene-Plugins-Official.git@cd6aad8599f8197fb33d631ec245af65e5964852#subdirectory=plugins/tools/dataset-preparation",
  "duckdb==1.5.5",
  "httpx==0.28.1",
  "jsonschema==4.26.0",
  "mypy==2.3.1",
  "openapi-spec-validator==0.9.0",
  "pytest==9.1.1",
  "ruff==0.16.5",
]

[tool.hatch.build.targets.wheel]
packages = ["src/cyrene_catalyst"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
testpaths = ["tests"]
filterwarnings = [
  "ignore:Using `httpx` with `starlette.testclient` is deprecated:starlette.exceptions.StarletteDeprecationWarning",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["RUF002"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src/cyrene_catalyst"]

[[tool.mypy.overrides]]
module = ["cy_artifacts.*"]
ignore_missing_imports = true
